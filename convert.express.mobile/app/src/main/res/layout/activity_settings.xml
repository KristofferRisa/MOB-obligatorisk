<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical">

    <android.support.v7.widget.Toolbar
        android:id="@+id/my_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:elevation="4dp"
        android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"/>

    <CheckBox
        android:id="@+id/lagreCheckBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/my_toolbar"
        android:layout_marginStart="44dp"
        android:layout_marginTop="116dp"
        android:text="Lagre søk"
        android:textSize="25sp" />

    <Button
        android:id="@+id/slettButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/textView2"
        android:layout_alignParentBottom="true"
        android:layout_alignStart="@+id/lagreCheckBox"
        android:layout_marginBottom="163dp"
        android:background="@android:color/holo_red_light"
        android:text="Slett historikk" />

    <TextView
        android:id="@+id/textView2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/lagreCheckBox"
        android:layout_below="@+id/my_toolbar"
        android:layout_marginTop="18dp"
        android:text="Settings"
        android:textSize="50sp" />

    <Button
        android:id="@+id/lukkButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/slettButton"
        android:layout_alignStart="@+id/slettButton"
        android:layout_alignTop="@+id/slettButton"
        android:layout_marginTop="79dp"
        android:text="Lukk" />


</RelativeLayout>

